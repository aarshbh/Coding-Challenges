let a;
let b=10;
console.log(
    a ?? b
);

//   ### TASKS ###   //
/*
1. Create a undefined variable called course. Use the
   Console to display the course or otherwise the
   default text "Please select a course !".



2. What will the following return
*/

let courseProgress = 0;
console.log(
    courseProgress ?? "Start the Course"
);

//   <<< SOLUTIONS >>>   //

let course;
console.log(
    course ?? "Please select the course"
);
